# tren-casa

This repository contains the `home-departure-board` app.

For Vercel, import this GitHub repository and set the project Root Directory to:

```text
home-departure-board
```

The deploy settings and serverless API entry are committed inside that folder.
